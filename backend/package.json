{
  "name": "catherd-backend",
  "version": "0.0.0",
  "scripts": {
    "compile": "tsc -p src/",
    "prestart": "npm run compile",
    "start": "node src/app/server.js"
  },
  "dependencies": {
    "express": "^4.14.1"
  },
  "devDependencies": {
    "@types/express": "^4.0.35",
    "typescript": "^2.1.6"
  }
}
